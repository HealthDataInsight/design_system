<% controller.sidebar_sections.each do |section| %>
  <%= ds_sidebar(section[:heading], type: :heading) %>
  <ol class="<%= brand %>-list">
    <% section[:items].each do |item| %>
      <%= ds_sidebar(item) %>
    <% end %>
  </ol>
<% end %>
